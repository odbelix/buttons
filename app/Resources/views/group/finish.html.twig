{% extends 'base.html.twig' %}

{% block body %}
    <h1>Grupo {{group.id}} | {{group.createdat|date('Y-m-d')}}</h1>
    <ul>
        <li>
            {{ form_start(form) }}
                <input type="submit" value="Terminar">
            {{ form_end(form) }}
        </li>
    </ul>
{% endblock %}
